/* Создайте анонимную функцию, которая должна выводить сообщение 'message in console' в консоль. 
Используйте ее как обработчик события click для кнопки. */

const message = function () {
    const message = 'message in console';
    console.log(message);
}